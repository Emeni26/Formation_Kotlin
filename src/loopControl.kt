fun main(args: Array<String>) {

    loop@ for (count in 1..10) {

        for (count2 in 1..3) {
            println("count : $count")
        }
        if (count == 3)
        {
            break // afficher tous les nombres de la liste sauf 4
        }}

        println("end app") }







       /*  for (count in 1..10){

        if (count==4){
            continue // afficher tous les nombres de la liste sauf 4
        }
        println("count : $count")}*/


    /*for (count in 1..10){

        println("count : $count")
        if (count==4){
        break //pour dire rant qu'on a trouvé le num recherché , pas la peine de parcourir le reste de la liste
    }}*/
   // println("end app")
//}